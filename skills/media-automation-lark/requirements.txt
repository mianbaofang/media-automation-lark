# 自媒体自动化工作流（飞书 CLI 版）— Python 依赖
# 安装：pip install -r requirements.txt
# 场景 C 使用 pypdf 作为 PDF 回退；MarkItDown（markitdown[all]）是文件转 Markdown 的工具。

requests
feedparser
beautifulsoup4
pandas
openpyxl
python-docx
pypdf
markitdown[all]
